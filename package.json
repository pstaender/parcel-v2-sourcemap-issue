{
  "name": "parcel-v2-sourcemap-issue",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build --no-cache --no-minify ./src/good.html; parcel build --no-cache --no-minify ./src/bad.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-nightly.462",
    "sass": "^1.29.0"
  }
}
